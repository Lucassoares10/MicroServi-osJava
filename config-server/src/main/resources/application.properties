spring.application.name=hr-config-server
spring.cloud.config.server.git.default-label=main #para o projeto considerar o main inves do master
server.port=8888

#passando repositorio do server config, para o config ser pego no git hub.
#passando user e senha porque o projeto é privado.
#passando as variaveis nas variaveis de ambiente do sistema operacional.
spring.cloud.config.server.git.uri=https://github.com/Lucassoares10/ConfigMicroServi-os
spring.cloud.config.server.git.username=${GITHUB_USER}
spring.cloud.config.server.git.password=${GITHUB_PASS}
